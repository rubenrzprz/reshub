spring:
  application:
    name: reshub-test

logging:
  level:
    com.rubenrzprz.reshub: DEBUG

security:
  jwt:
    secret: "test-secret-key-with-at-least-32-characters"
    issuer: "reshub-test"
    expiration-minutes: 60
features:
  enforce-agency-hotel-auth: false
