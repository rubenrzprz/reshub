spring:
  application:
    name: reshub

server:
  port: 8080

security:
  jwt:
    secret: "${JWT_SECRET:}"
    issuer: "reshub"
    expiration-minutes: 60
features:
  enforce-agency-hotel-auth: false
